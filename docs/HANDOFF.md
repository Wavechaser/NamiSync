# Session Handoff

Status (2026-08-27): power-loss recovery and the checkpoint-4 planning-source
ownership prerequisite are complete in the commit containing this handoff.
Checkpoint 4 remains before its first mandatory reservation-model commit. The
production command map still has exactly nine rows; no task command, lifecycle,
analytical constant, fixture, validator hash, measurement, or BR-G-45
acceptance has activated.

## Recovery and safe lineage

- The complete interrupted 19-path state remains preserved at Git object
  `33f699448b5940b2aa4b0464b3a238297c68ab86` and anchored by branch
  `codex/recovery-power-loss-20260827`. Do not delete either recovery reference
  until checkpoint 4 is safely past its first model commit.
- Recovery restarted from `b457ef9`. The coherent adapter exception-retirement
  work landed as `1e52fb9`; the independently reproduced stale canonical-plan
  vector fix landed as `a8be549`. The current source wall was rebuilt from the
  contracts rather than accepting the interrupted provisional model.
- The recovered model totals, formulas, fixtures, and hashes remain
  non-authoritative and were not copied into active source or documentation.

## Planning-source ownership closure

- One outer `PlanReviewAdmission` now follows a plan session through source and
  target scans, prior correspondence, planner policy/assignment/operations,
  initial selection, observation, and preflight.
- Independent raw populations refuse before first-excess construction;
  semantic domain and informational facts accumulate once across the complete
  session; simultaneously retained shallow container owners charge fixed
  eight-byte reference slots on disposable producer/capture forks.
- Exact scan, mapping, options, assignment, plan, selection, observed-world,
  and verdict graphs are reconstructed from declared typed fields. Hidden
  instance dictionaries, hostile deep-copy behavior, raw mapping wrappers,
  callback mutation, request drift during policy access, and partial result
  publication are rejected.
- Scanner sort/final overlap, planner dependency and assignment copies,
  selection quarantine/exclusion ownership, observer maps, and preflight
  refusal key/dict/sort/tuple transitions are admitted before allocation. A
  first limit excess returns the matching typed `REFUSED+UNRUN` result and
  never calls the plan saver.
- Independent adversarial review found and closed late dependency/refusal-key
  charges, an unused but retained selection-exclusion graph, hostile equality
  before callback-world validation, and policy-driven request identity drift.
  An 11-boundary end-to-end matrix proves exact refusal and downstream stop at
  the named high-risk producer/copy/final transitions; protected scanner/
  planner/observer/preflight behavior is pinned equal to ordinary behavior.

This is finite source and ownership enforcement, not the checkpoint-4
complete-object model. It does not price object headers, container capacity,
text/codec/native/browser copies, exception frames, task artifacts, or
multi-session overlap and cannot be used as reservation evidence.

## Verification

- Focused plan-wall plus scanner/planner/preflight/workflow files: **316
  passed**.
- Core/scanner/planner/preflight/workflow department union: **2,220 passed, 1
  skipped, 2,705 deselected**.
- Required-Node ordinary repository suite: **4,894 passed, 4 skipped, 28 headed
  deselected**.
- Import architecture: **11 contracts kept, 0 broken**.
- Protected settlement oracle: **30 scenarios × 3 runs**, identical accepted
  traces.
- `git diff --check` is clean apart from Git's existing LF-to-CRLF notices.

## Next safe work

Close the remaining raw exception owners before freezing the model. In
particular, workflow recording boundaries still retain `enter_error`/
`exit_error`; plan and destination-policy collaborator failures can escape with
producer frames; callback retry closures retain first raw failures; dispatcher
worker/cancellation publication can close over a raw `BaseException`. Land
causal fixes separately with weak-reference/traceback regressions and ordinary
semantic parity. Do not combine them with model constants.

Then execute checkpoint 4's mandatory stops in order:

1. `test(web): pin task artifact reservation model`
2. `feat(web): install dormant task lifecycle`
3. `feat(web): retain multi-session task artifacts`

The first stop must derive a fresh complete ownership graph and validator from
the active source and `M1_BRIDGE.md`/`DEFENSE.md`, not from the recovery
snapshot. The second must preserve the exact nine-row command map and current
one-session behavior. Only the third may activate the coherent 12-row map and
six task rows. BR-G-45 and SH-G-15 remain open throughout the prerequisite
work.
