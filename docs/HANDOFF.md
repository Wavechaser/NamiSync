# Latest session handoff

## R7 consolidation through R7-3 (2026-09-17)

Authorized batch: R7-1–R7-8 and R7-G under execute-task; deferred items/F1
hardening, push and PR excluded. Parent M1_PLAN and detailed
[ablation register](M1_7_ABLATION_STUDY.md) own scope/gates.

Startup baseline/policy: ee8d861. R7-1: efb5dc7, synthetic/live fixture split.
R7-2: 884813d, direct canonical path/node ordering. Both reviewed and committed;
postcommit identity checks pass. Evidence is under
build/m1-7-ablation/implementation/{startup,r7-1,r7-2}/.

R7-3 is the commit containing this handoff, titled
`refactor(presentation): Retire unused projection replacement`. It removes only
the unused method and migrates four test consumers to active selection,
acquisition and revision transitions. Registry publication and constructor
validation remain unchanged. Failed selection transformation has an explicit
complete-view preservation witness. Final P171, WI2482/1 historical-artifact
skip, L12 and installed H30 pass. H predates only ordinary test assertion edits;
its product and headed harness bytes remain identical. Final P/WI cover those
assertions. Independent review approved source, tests and evidence hashes.
Four copied candidate controls pass; reset-sort, malformed text/topology and
early-publication faults reach and fail the intended retained assertions.
Exact commands, patches, loaded paths/hashes and raw logs are retained under
build/m1-7-ablation/implementation/r7-3/manifest.md.

Measurement policy: affected Tier 1 local guards per checkpoint, one full
Q-final at R7-G. Local checks do not renew acceptance or pool children.
R7-2 memory baseline/candidate maxima were 278835200/285605888 bytes against
335544320; no improvement claimed. R7-3 changed no timed child body and uses
the declared no-timing decision. P9/legacy evidence remains immutable.
Use normal external TEMP and desktop-capable escalated verification when
needed; repository-local TEMP trips custody's intentional cache guard.
Historical-artifact skip lacks NAMISYNC_M1_7_READINESS_PATH and is never Q.

Next: verify R7-3 committed identity, refresh R7-4 design inspected at 884813d,
freeze baseline validator and finite public-entry corruption corpus, then
consolidate only validator-local receipt checks. Preserve `_validate_children`
signature because the retained R7-2 evidence checker uses it. No timed child
changes are authorized for R7-4. Continue the authorized batch; AGENTS
stops/recurrence/recovery remain binding and latent defects are report-only.
Preserve task-owned control copies through final closeout. No task branch or
worktree has been created; integration is directly on milestone1.
