# Session Handoff

Status (2026-08-13): early-shell realignment checkpoints 1-8 have landed.
`M1_BRIDGE.md` alone owns the protocol and every BR-G gate. Checkpoint 6 closes
the installed real-WebView2 browser-witness migration; SH-G-8's complete
normal-envelope evidence remains open, and Slices 5-8 remain product work.

## Delivered

- Reduced `M1_SHELL.md` to Stage 6 order, host/package placement,
  launcher/packaging decisions, SH-G definitions, and the BR-G dependency map.
  It links to bridge and visual authorities instead of restating their
  contracts.
- Moved the exact bridge error vocabulary and remaining reverse delegates into
  `M1_BRIDGE.md`, and made its sole-authority standing explicit.
- Repointed `M1_PLAN.md`, `INTERFACES.md`, `DESKTOP_UI.md`,
  `ARCHITECTURE.md`, README, and `FEATURES.md` to that authority. The exact
  authored palette and material/motion contract now live in
  `DESKTOP_UI.md`, not the shell delivery plan.
- Recorded the competing-authority defect in `BUGS.md`. Numeric-hole recovery
  and command-specific `start_plan` revision are ratified with landed named
  regressions.
- Migrated the named transport and drain-manager browser witnesses to the
  clean installed-wheel WebView2 harness. They now exercise the production
  bridge and renderer; browserless/Node probes remain supplemental.
- Marked and renamed all seven Node-backed probes as supplemental, limited
  discovery to `node` on declared `PATH`, and made the authoritative ordinary
  lane independent of Node availability.
- Added a literal witness table for every approved public-view dataclass and
  routed every witness plus hostile return types through production dispatch.

## Verification And Review

- The focused installed-wheel transport headed suite passed: 6 passed and 8
  deselected. The combined transport, shell, materials, and component-gallery
  headed suites passed: 16 passed and 24 deselected.
- The corresponding ordinary headed-harness suites passed: 24 passed and 16
  deselected.
- The public-view manifest/dispatch suites passed: 175 passed. The
  authoritative non-Node frontend/transport/icon lane passed: 133 passed and 7
  supplemental probes deselected.
- Active-document contradiction searches excluded `docs/obsolete/` and the
  temporary root reference and found no actionable stale command-table count,
  reverse delegation, mirrored-boundary language, false SH-G-8 closure,
  Node-only authority, or competing retry/sequence/lifecycle contract. The
  remaining matches are explicit negative/history statements or unrelated CLI
  command counts.
- `git diff --check` passed.

## Immediate Context

Checkpoint 9 alone may close SH-G-8, and only after the complete BR-G-42 normal four-task
duration/rate/latency/memory envelope passes. Preserve the explicit separation
between terminal-session release and explicit task close.

The root `M1_SHELL.md` reference and `S0-S3_REALIGNMENT.md` plan are
temporary untracked files. They are excluded from active contradiction searches
and must not be edited or committed.
