# Session Handoff

Status (2026-08-27): checkpoint **3.3 is complete**. Checkpoints 0–3 and the
independently reviewed 3R remediation are delivered. Next is checkpoint 4's
first required commit, `test(web): pin task artifact reservation model`.
The production command map remains the existing nine rows; no task-lifecycle
or product-surface target has been activated by this cleanup.

## Delivered

- Removed the private Python v3/v4 event decoder and the browser's private v4
  validator, including every helper used only by those branches.
- Replaced the positive private compatibility fixture with all-family retired-
  version refusal through the live decoder and packaged task-update route.
  Numeric Progress remains refused; canonical v5 remains accepted.
- Added failing-first source-removal guards and isolated in-memory route and
  helper-reintroduction counterexamples. The drain probe now co-batches a
  valid prefix with malformed v3/v4 events and proves no delivery or cursor
  advance before clean replay.
- Independent review found no production defect. Its two test-quality findings
  (non-isolated source mutation and single-event batch evidence) were corrected
  and re-reviewed before closure. Closed the matching schema-acceptance entry
  in BUGS and removed stale private-decoder claims from active owner documents.

## Verification

- Initial source-removal witnesses: **2 expected failures** before deletion.
- Focused core/event/browser run with required bundled Node: **899 passed**.
- Core/interfaces departments: **2,428 passed, 1 privilege skip**.
- Ordinary required-Node suite: **4,524 passed, 4 privilege skips,
  28 headed deselected** (189.83 s). The skips require unavailable Windows
  symlink/reparse privileges; no required Node gate skipped.
- Import architecture: **11 kept, 0 broken**.
- Protected settlement oracle: **30 scenarios × 3**, identical normalized
  traces and baseline parity. Whitespace validation passed.
- No headed acceptance was needed or claimed for unreachable-source removal.
  BR-G-36's later follow/execution portions, BR-G-45 and SH-G-15 remain open.

## Next checkpoint and preserved boundaries

Follow [M1_SHELL_H2.md](M1_SHELL_H2.md#4-install-task-centric-lifecycle-and-compact-artifacts)
one independently reviewed and committed stop at a time. Checkpoint 4 first
freezes the complete analytical artifact graph, constants/formula, maximum
fixtures and independent validator before measuring or activating anything;
then installs dormant lifecycle machinery; only its third commit activates
the coherent 12-row command map and task UI. The accepted sorting and
rebaseline additions remain assigned to checkpoints 7/9 and 10 in H2.

All changed files belong to checkpoint 3.3; no inherited dirty work was present.
The protected settlement oracle/baseline/assertions, frozen transport authority,
and old-epoch witness are untouched. The epoch-5 witness SHA-256 remains
`52f80f8539b863da0a357ba4a47c20a32cb77a5a14db9194d5adf98e31c538d9`.
The active pair remains ledger v4/history v6 at shared data epoch 6; old
pairs require explicit paired archive/reset. No user database was reset.

## Separately deferred context

- The installed-wheel event diagnostic's producer/page/parent fixture migration
  remains unassigned under BR-G-42; it is not current-v5 timing acceptance.
- `_settle_execute_resume_failure` pre-entry/resume sink behavior remains an
  inspection-only concern, not a reproduced or repaired 3.3 defect.
- The prior 3R closure and preserved capture provenance remain available at
  `e4a1ae0:docs/HANDOFF.md` and its historical references.
