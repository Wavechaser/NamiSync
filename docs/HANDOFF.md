# Latest session handoff

## M1-7 test/evidence machinery reassessment (2026-09-17)

User redirected the goal after R7-4: investigate larger reductions to added
tests/evidence machinery, including whether capabilities should exist at all.
The shell itself is fine. R7-5–R7-8 and R7-G implementation is suspended.
This is investigation authority, not permission to delete tests or change SLOs.
The active RI-1–RI-4 register and report are at the top of
[M1_7_ABLATION_STUDY.md](M1_7_ABLATION_STUDY.md); M1_PLAN records the direction.

Baseline: clean milestone1 at 95f31e1. Delivered commits remain ee8d861 startup,
efb5dc7 R7-1, 884813d R7-2, fff3136 R7-3, 95f31e1 R7-4. R7-4's reviewed exact
source passed S55/1 historical-artifact skip, I1675/1 skip, 59 baseline comparison
cases and three fault controls. This investigation did not rerun those gates.

Read-only corpus: original 40ca76f..5986c57 test/evidence additions, integrated
R7 changes, repository consumers, benchmark/validator/driver/probe mechanisms,
DEFENSE/PRESENTATION/TESTS and relevant earlier reduction dispositions. Source
and Git census plus independent policy/harness reviews support the proposal.
R7-1–R7-4 removed net76 product lines but added net134 test/helper lines; no
literal transfer of production algorithms was found. The three scale machinery
files currently total7872 textual lines. Counts are diagnostic, not savings.

Recommendation for decision: replace the evidence lifecycle while preserving
35 numeric cases initially. Retire separate readiness certification, durable
partial-index state/API, live legacy compatibility after pinned archival, and
optional Plan-again tracing with its four consumers. Keep genuine measurement,
source/wheel/install/runtime identity, complete terminal results and independent
fixture/statistical verdicts. Reopen bounded A6 scenario/driver cleanup. Explicit
losses: earlier setup-failure detection, structured interrupted-run inspection,
and phase-level trace diagnosis. No replacement journal/general framework.
A stronger alternative reduces numeric cases, but each dropped timing/memory
claim must be explicitly withdrawn; representative timings do not certify it.

R7-5/R7-8 should be superseded if that proposal is accepted; R7-6/R7-7 remain
optional product maintenance outside the clarified goal. Product redesigns
A5/A7/A8/broaderA12 are not necessary for test-machine reduction. Full-current-
source Q acceptance is still pending, not silently waived by this study.
P9/legacy artifacts and all previous ignored evidence remain untouched.

Evidence: build/m1-7-ablation/reassessment/manifest.md, inventory.py/json,
families.py/json and replay_history.py/historical_replay.json. Pinned historical
validators reproduced the legacy maximum-budget rejection and P9's 35 accepted
observations using unchanged JSON files. This is historical interpretation,
not fresh measurement or current-source acceptance. Only documentation changed.
No performance tests, benchmark runs, fault experiments, production/test edits
or cleanup in this study. The proposal explicitly pins shared contract identity
outside producer output and names the user-owned archival exit decision.
Next: user decision on capability retirement and retained numeric claims, then
write a bounded replacement implementation plan before changing mechanisms.
